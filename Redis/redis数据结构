1. 概览
    -- String
        - 存储：字符串、整数或者浮点数
        - 操作：对字符串或者字符串一部分执行操作，整数或者浮点数 执行 自增或者自减
    -- List
        - 存储：链表，每一个节点包含一个字符串
        - 操作：从两端推入或则弹出元素；根据偏移量对链表进行修剪；读取多个或则单个元素；根据值来查找或者移除元素
    -- Hash
        - 存储：包含键值对的无序散列表
        - 操作：添加、获取、移除单个键值对；获取所以键值对
    -- Set
        - 存储：字符串的无序集合，相当于Java中的Set
        - 操作：添加、获取、移除单个元素；检查一个元素是否存在于某个集合中；计算交集、并集、差集；从集合里随机获取元素
    -- ZSet
        - 存储：字符串成员(member)与浮点数分值(score)之间的有序映射，元素由分值的大小决定排列顺序
        - 操作：添加、获取、删除单个元素；根据分值范围(range)或者成员来获取元素